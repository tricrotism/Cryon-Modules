rootProject.name = "cryon-jumppads"

// Standalone builds resolve cryon-jumppads-api from mavenLocal (run `publishApis` first). From the
// Cryon-Modules composite root the sibling build is substituted automatically — no round-trip needed.
